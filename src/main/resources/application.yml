spring:
  application:
    name: shopping-list
  datasource:
    url: jdbc:postgresql://localhost:5432/shopping
    username: shopping
    password: shopping
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always


keycloak:
  realm: shopping-list
  auth-server-url: http://localhost:8090/auth
  ssl-required: external
  resource: spring-boot-keycloak
  credentials:
    secret: 6rRwvnETqeetTpY9VFoHapcJZJkIDVWl
  use-resource-role-mappings: true

logging:
  level:
    org.springframework.security: DEBUG
    org.example.shoppinglist: DEBUG